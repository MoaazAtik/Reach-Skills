/*
/// Todo: Set up the development config to use local or test data
/// instead of the main remote data.
/// Currently, it's commented out to prevent confusing and misuse.

/// Development config entry point.
/// Launch with `flutter run --target lib/main_development.dart`.
/// Uses local or test data.
void main() async {
  await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform);

  runApp(
    MultiProvider(providers: remoteProviders, child: const ReachSkillsApp()),
  );
}
*/
